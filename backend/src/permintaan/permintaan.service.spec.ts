it('should create permintaan with multiple items', async () => {
  // Mock barangRepo.findByIds, permintaanRepo.save, detailRepo.save
  // Test valid input, expect permintaan dan detail tersimpan
});
